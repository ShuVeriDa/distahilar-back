# ⚡ Быстрое решение проблемы "Too many database connections"

## 🔴 Проблема
```
PrismaClientInitializationError: Too many database connections opened
```

## ✅ Решение (5 минут)

### Шаг 1: Обновите DATABASE_URL в Vercel

1. Откройте vercel.com → ваш проект → **Settings** → **Environment Variables**

2. Найдите `DATABASE_URL` и **замените** на URL с connection pooling:

#### Если используете Supabase:
```
KEY: DATABASE_URL
VALUE: postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:6543/postgres?pgbouncer=true&connection_limit=1
```

**Важно**: 
- Используйте **порт 6543** (не 5432)
- Добавьте `?pgbouncer=true&connection_limit=1`

**Где найти Pooling URL в Supabase:**
1. Supabase Dashboard
2. Project Settings → Database → Connection Pooling
3. Скопируйте "Connection string" из секции "Session mode"

#### Если используете Neon:
```
KEY: DATABASE_URL  
VALUE: [ваш существующий URL]?connection_limit=1
```

#### Если используете Vercel Postgres:
Используйте `POSTGRES_PRISMA_URL` вместо `POSTGRES_URL`

---

### Шаг 2: Убедитесь, что есть другие переменные

Добавьте если отсутствуют:

```
KEY: NODE_ENV
VALUE: production

KEY: FRONTEND_URL
VALUE: https://distahilar-front.vercel.app

KEY: JWT_ACCESS_SECRET
VALUE: [ваш секрет или сгенерируйте новый]

KEY: JWT_REFRESH_SECRET
VALUE: [ваш секрет или сгенерируйте новый]
```

---

### Шаг 3: Пересоберите и задеплойте

```bash
# Локально:
cd back
npm run build

# Или просто коммит и пуш:
git add .
git commit -m "fix: database connection pooling for serverless"
git push
```

Vercel автоматически пересоберет проект.

---

### Шаг 4: Проверка

1. Дождитесь завершения деплоя
2. Проверьте логи: `vercel logs --follow`
3. Откройте фронтенд и попробуйте залогиниться
4. Ошибка должна исчезнуть! ✅

---

## 📋 Чек-лист

- [ ] DATABASE_URL обновлен на порт **6543** (для Supabase)
- [ ] Добавлен параметр `?pgbouncer=true&connection_limit=1`
- [ ] Все необходимые переменные окружения установлены в Vercel
- [ ] Проект пересобран и задеплоен
- [ ] Проверены логи - нет ошибок с БД

---

## 🆘 Если не помогло

1. Проверьте, что вы используете **Connection Pooling URL**, а не Direct URL
2. Убедитесь, что connection pooling **включен** у вашего БД провайдера
3. Посмотрите подробную инструкцию: `VERCEL_ENV_VARIABLES.md`
4. Проверьте логи Vercel на наличие других ошибок

---

## 💡 Почему это важно?

Serverless-функции создают новое подключение к БД для каждого запроса. Без connection pooling вы быстро исчерпаете лимит подключений PostgreSQL (обычно 100).

**Connection pooling** решает эту проблему, переиспользуя существующие подключения.

---

## Дополнительная информация

- 📖 Полная документация: `VERCEL_DB_SETUP.md`
- 🔧 Все переменные окружения: `VERCEL_ENV_VARIABLES.md`
- 💻 Изменения в коде уже применены в `prisma.service.ts` и `api/index.ts`

