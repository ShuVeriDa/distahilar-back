# Copy this file to ".env" and fill in the values
# For production, never commit your real secrets to VCS

# -----------------------------------------
# Runtime
# -----------------------------------------
# Node environment: development | production | test
NODE_ENV=development

# Port for NestJS to listen on
PORT=9555

# Frontend origin for CORS (must match your Next.js app URL)
# Example (local): http://localhost:3000
FRONTEND_URL=http://localhost:3000

# -----------------------------------------
# Database (PostgreSQL)
# -----------------------------------------
# Full PostgreSQL connection string.
# Prefer a connection pool for serverless (pgbouncer).
# Example (local):
# postgresql://USER:PASSWORD@localhost:5432/DB_NAME?schema=public
# Example (Supabase pooled):
# postgresql://USER:PASSWORD@HOST:6543/DB_NAME?pgbouncer=true&connection_limit=1
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB_NAME?schema=public

# -----------------------------------------
# JWT (Authentication)
# -----------------------------------------
# Use long, random strings. Keep them secret.
JWT_ACCESS_SECRET=replace_with_long_random_string
JWT_REFRESH_SECRET=replace_with_long_random_string

# -----------------------------------------
# Cloudinary (Media uploads)
# -----------------------------------------
# Required for file/image/video uploads and optimization.
# Create an account on https://cloudinary.com and get your credentials.
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# -----------------------------------------
# Notes
# -----------------------------------------
# - API will run at http://localhost:${PORT} with global prefix /api
# - Swagger is enabled only when NODE_ENV !== production at /api
# - Ensure FRONTEND_URL and CORS match your deployed frontend


